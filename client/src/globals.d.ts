/**
 * Global constants injected by Vite's `define` config.
 * See vite.config.ts — values come from shared/constants.json.
 */

/** Port offset from server port to client port (e.g. 1000). */
declare const __CLIENT_PORT_OFFSET__: number;
